// create server & client folder

//! server
// npm init // package-lock.json is created and is going to keep track of all modules that will be installed//
//npm install express // node_modules are getting created
//? create app.js and set it up:
        // const express = require('express') //this is a function block that has to be called //layer built on the top of the Node js that helps manage servers and routes.
        // const app = express() //invoking the function will return an object (instance of express)
        // let port = 8080

        // app.get('/',(req,res)=>{ //route handler
        //     res.send('this is the "/" route')
        // })

        // app.listen(port,()=>{
        //     console.log(`listening on port: ${port}`)
        // })
//? npm install pg pg-hstore // a collection of Node. js modules for interfacing with a PostgreSQL database.